server:
  port: 9876

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank
    username: postgres
    password: cdac
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: never


